#include "color.h"

Color createColor(int red, int green, int blue, int alpha){

    Color color;
    color.red = red;
    color.green = green;
    color.blue = blue;
    color.alpha = alpha;

    return color;

}
